{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna clean --yes",
    "bootstrap": "npm run clean && lerna bootstrap --use-workspaces",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
